apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: metaflow
  labels:
    app: metaflow-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: metaflow-api
