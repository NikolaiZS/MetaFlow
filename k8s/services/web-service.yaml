apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: metaflow
  labels:
    app: metaflow-web
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: metaflow-web
